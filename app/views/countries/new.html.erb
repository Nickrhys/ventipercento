<%= form_for @country, remote: true do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>


<ul id="countries">
  <%render @countries %>
</ul>

<%= link_to "View all countries", countries_path %>
